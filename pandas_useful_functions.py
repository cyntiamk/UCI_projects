unique_items_df = main.df.drop_duplicates(["Column"], keep="last")
keep="first" # (lets you filter out then non unique values)

empty_list = [value for value in list_of_values]
	
new_df = df1.add(df2, fill_value=0)

# Drop a 'specific' row that is contained within our group by calling the index 
grouped_df = grouped_df.drop(grouped_df.index[3])

del mergedORcombined_df["column name"]

